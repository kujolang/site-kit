{
  "$schema": "../../schemas/component.schema.json",
  "name": "Product Card",
  "description": "A semantic commerce product summary with media, price, and actions.",
  "props": {
    "name": "string",
    "price": "string",
    "imageAlt": "string",
    "availability": "string"
  },
  "variants": [
    "default",
    "compact",
    "featured"
  ],
  "requiredFields": [
    "name",
    "price"
  ],
  "accessibility": {
    "semantics": "Use native semantic elements in Product Card templates before ARIA.",
    "keyboard": "Interactive descendants must be reachable by keyboard and expose visible focus.",
    "focus": "Uses :focus-visible styles in component CSS.",
    "notes": [
      "Color is never the only communication channel.",
      "Motion must respect prefers-reduced-motion where animation exists."
    ]
  },
  "tokenDependencies": [
    "border.1",
    "border.default",
    "component.card.gap",
    "component.card.padding",
    "focus.offset",
    "focus.ring",
    "focus.width",
    "font.mono",
    "radius.0",
    "shadow.none",
    "space.2",
    "space.3",
    "space.4",
    "state.danger",
    "surface.card",
    "surface.subtle",
    "text.muted",
    "text.primary",
    "text.secondary",
    "type.size.xl",
    "type.size.xs",
    "type.weight.bold"
  ],
  "responsive": {
    "strategy": "Intrinsic layout with logical properties, responsive grids, and container queries where useful.",
    "breakpoints": [
      "sm",
      "md",
      "lg"
    ]
  },
  "slots": [
    "media",
    "summary",
    "actions"
  ],
  "examples": [
    {
      "id": "product-card-default",
      "name": "Standalone",
      "props": {
        "name": "project",
        "price": "Example"
      },
      "htmlFile": "example.html",
      "notes": "Consumer-authored reference, no automatic prop binding. Prefix IDs before repeating."
    }
  ],
  "allowedChildren": [
    "media",
    "summary",
    "actions"
  ],
  "propertyContracts": {
    "name": {
      "type": "string",
      "required": true,
      "default": null,
      "allowedValues": null,
      "binding": {
        "selector": "input, .sk-icon",
        "html": "native name; Icon data-icon-name is metadata only"
      },
      "stateOwner": "consumer",
      "example": "project"
    },
    "price": {
      "type": "string",
      "required": true,
      "default": null,
      "allowedValues": null,
      "binding": {
        "selector": ".sk-product-card",
        "html": "consumer-authored content; no attribute switch or runtime binding; see canonical example"
      },
      "stateOwner": "consumer",
      "example": "Example"
    },
    "imageAlt": {
      "type": "string",
      "required": false,
      "default": null,
      "allowedValues": null,
      "binding": {
        "selector": ".sk-product-card",
        "html": "consumer-authored content; no attribute switch or runtime binding; see canonical example"
      },
      "stateOwner": "consumer",
      "example": "Example"
    },
    "availability": {
      "type": "string",
      "required": false,
      "default": null,
      "allowedValues": null,
      "binding": {
        "selector": ".sk-product-card",
        "html": "consumer-authored content; no attribute switch or runtime binding; see canonical example"
      },
      "stateOwner": "consumer",
      "example": "Example"
    }
  },
  "variantContracts": {
    "default": {
      "kind": "alias",
      "binding": null,
      "aliasOf": "base",
      "notes": "Compatible legacy name for the base appearance; no distinct attribute behavior."
    },
    "compact": {
      "kind": "alias",
      "binding": null,
      "aliasOf": "base",
      "notes": "Compatible legacy name for the base appearance; no distinct attribute behavior."
    },
    "featured": {
      "kind": "alias",
      "binding": null,
      "aliasOf": "base",
      "notes": "Compatible legacy name for the base appearance; no distinct attribute behavior."
    }
  },
  "guidance": {
    "use": "Present product title, description, price and actions beside media.",
    "avoid": "A cart item, inventory model or automatic media renderer."
  },
  "slotContracts": {
    "media": {
      "owner": "consumer",
      "content": "Trusted authored semantic HTML; preserve child structure in example.html. Text data must be escaped.",
      "example": "example.html"
    },
    "summary": {
      "owner": "consumer",
      "content": "Trusted authored semantic HTML; preserve child structure in example.html. Text data must be escaped.",
      "example": "example.html"
    },
    "actions": {
      "owner": "consumer",
      "content": "Trusted authored semantic HTML; preserve child structure in example.html. Text data must be escaped.",
      "example": "example.html"
    }
  },
  "historicalSources": [],
  "category": "Commerce"
}
