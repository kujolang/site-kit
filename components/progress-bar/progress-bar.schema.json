{
  "$schema": "../../schemas/component.schema.json",
  "name": "Progress Bar",
  "description": "A horizontal progress meter for determinate completion.",
  "props": {
    "label": "string optional",
    "variant": "string optional"
  },
  "variants": [
    "determinate",
    "compact"
  ],
  "requiredFields": [],
  "accessibility": {
    "semantics": "Use native semantic elements before ARIA.",
    "keyboard": "Interactive descendants must be reachable by keyboard and expose visible focus.",
    "focus": "Uses :focus-visible styles in component CSS.",
    "notes": [
      "Color is never the only communication channel.",
      "Motion must respect prefers-reduced-motion where animation exists."
    ]
  },
  "tokenDependencies": [
    "surface.page",
    "surface.card",
    "surface.raised",
    "surface.subtle",
    "text.primary",
    "text.secondary",
    "text.muted",
    "border.default",
    "border.subtle",
    "focus.ring",
    "space.4",
    "radius.0",
    "font.mono"
  ],
  "responsive": {
    "strategy": "Intrinsic layout with logical properties, responsive grids, and container queries where useful.",
    "breakpoints": [
      "sm",
      "md",
      "lg"
    ]
  },
  "slots": [
    "label",
    "content",
    "actions"
  ],
  "examples": [
    {
      "name": "Default",
      "props": {
        "label": "Progress Bar",
        "variant": "determinate"
      },
      "notes": "Use semantic landmarks and tokenized styling."
    }
  ],
  "allowedChildren": [
    "label",
    "content",
    "actions"
  ]
}
