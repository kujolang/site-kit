{
  "$schema": "../../schemas/component.schema.json",
  "name": "Tooltip",
  "description": "A short contextual text hint for an adjacent control.",
  "props": {
    "label": "string optional",
    "variant": "default|compact"
  },
  "variants": [
    "default",
    "compact"
  ],
  "requiredFields": [],
  "accessibility": {
    "semantics": "Use native semantic elements before ARIA.",
    "keyboard": "Interactive descendants must be reachable by keyboard and expose visible focus.",
    "focus": "Uses :focus-visible styles in component CSS.",
    "notes": [
      "Color is never the only communication channel.",
      "Motion must respect prefers-reduced-motion where animation exists."
    ]
  },
  "tokenDependencies": [
    "border.1",
    "border.default",
    "focus.offset",
    "focus.ring",
    "focus.width",
    "motion.base",
    "motion.ease",
    "space.2",
    "space.3",
    "surface.card",
    "text.primary",
    "type.size.sm",
    "z.dropdown"
  ],
  "responsive": {
    "strategy": "Intrinsic layout with logical properties, responsive grids, and container queries where useful.",
    "breakpoints": [
      "sm",
      "md",
      "lg"
    ]
  },
  "slots": [
    "label",
    "content",
    "actions"
  ],
  "examples": [
    {
      "id": "tooltip-default",
      "name": "Standalone",
      "props": {
        "label": "Info",
        "variant": "default"
      },
      "htmlFile": "example.html",
      "notes": "Consumer-authored reference, no automatic prop binding. Prefix IDs before repeating."
    }
  ],
  "allowedChildren": [
    "label",
    "content",
    "actions"
  ],
  "behavior": {
    "optionalScript": "dist/sitekit.js",
    "hooks": [
      "aria-describedby",
      "role=tooltip"
    ],
    "keyboard": "The tooltip is exposed while its trigger is focused and hides on blur or Escape.",
    "dismissal": "Tooltips are non-interactive hints and must not contain essential actions."
  },
  "propertyContracts": {
    "label": {
      "type": "string",
      "required": false,
      "default": null,
      "allowedValues": null,
      "binding": {
        "selector": "label, [aria-label], summary, button, a, span",
        "html": "textContent or accessible name appropriate to the element"
      },
      "stateOwner": "consumer",
      "example": "Tooltip"
    },
    "variant": {
      "type": "string",
      "required": false,
      "default": null,
      "allowedValues": [
        "default",
        "compact"
      ],
      "binding": {
        "selector": ".sk-tooltip",
        "html": "see variantContracts; names do not imply data-variant support"
      },
      "stateOwner": "consumer",
      "example": "default"
    }
  },
  "variantContracts": {
    "default": {
      "kind": "alias",
      "binding": null,
      "aliasOf": "base",
      "notes": "Compatible legacy name for the base appearance; no distinct attribute behavior."
    },
    "compact": {
      "kind": "alias",
      "binding": null,
      "aliasOf": "base",
      "notes": "Compatible legacy name for the base appearance; no distinct attribute behavior."
    }
  },
  "guidance": {
    "use": "Show advisory text on trigger focus/hover with Escape dismissal.",
    "avoid": "Essential content, interactive tooltip content or collision-aware positioning."
  },
  "slotContracts": {
    "label": {
      "owner": "consumer",
      "content": "Trusted authored semantic HTML; preserve child structure in example.html. Text data must be escaped.",
      "example": "example.html"
    },
    "content": {
      "owner": "consumer",
      "content": "Trusted authored semantic HTML; preserve child structure in example.html. Text data must be escaped.",
      "example": "example.html"
    },
    "actions": {
      "owner": "consumer",
      "content": "Trusted authored semantic HTML; preserve child structure in example.html. Text data must be escaped.",
      "example": "example.html"
    }
  },
  "historicalSources": [],
  "category": "Overlays"
}
