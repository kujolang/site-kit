{
  "$schema": "../../schemas/component.schema.json",
  "name": "Testimonial Rail",
  "description": "A scroll-safe testimonial layout for customer quotes and builder proof.",
  "props": {
    "heading": "string optional",
    "quotes": "array"
  },
  "variants": [
    "rail",
    "grid",
    "featured"
  ],
  "requiredFields": [
    "quotes"
  ],
  "accessibility": {
    "semantics": "Use native semantic elements in templates before ARIA.",
    "keyboard": "Interactive descendants must be reachable by keyboard and expose visible focus.",
    "focus": "Uses :focus-visible styles in component CSS.",
    "notes": [
      "Color is never the only communication channel.",
      "Motion must respect prefers-reduced-motion where animation exists."
    ]
  },
  "tokenDependencies": [
    "surface.page",
    "surface.card",
    "surface.raised",
    "text.primary",
    "text.secondary",
    "text.muted",
    "border.default",
    "focus.ring",
    "space.4",
    "radius.0"
  ],
  "responsive": {
    "strategy": "Intrinsic layout with logical properties, responsive grids, and container queries where useful.",
    "breakpoints": [
      "sm",
      "md",
      "lg"
    ]
  },
  "slots": [
    "heading",
    "quotes"
  ],
  "examples": [
    {
      "name": "Default",
      "props": {
        "label": "Testimonial Rail",
        "variant": "rail"
      },
      "notes": "Adapted from old-components into tokenized SiteKit styling."
    }
  ],
  "allowedChildren": [
    "heading",
    "quotes"
  ]
}
