{
  "$schema": "../../schemas/component.schema.json",
  "name": "Date Picker",
  "description": "A compact date-picker surface with a visible calendar grid.",
  "props": {
    "label": "string optional",
    "variant": "string optional",
    "value": "string optional",
    "min": "string optional",
    "max": "string optional"
  },
  "variants": [
    "calendar",
    "compact"
  ],
  "requiredFields": [],
  "accessibility": {
    "semantics": "Use native semantic elements before ARIA.",
    "keyboard": "Interactive descendants must be reachable by keyboard and expose visible focus.",
    "focus": "Uses :focus-visible styles in component CSS.",
    "notes": [
      "Color is never the only communication channel.",
      "Motion must respect prefers-reduced-motion where animation exists."
    ]
  },
  "tokenDependencies": [
    "action.primary",
    "action.primaryText",
    "border.1",
    "border.default",
    "focus.offset",
    "focus.ring",
    "focus.width",
    "font.mono",
    "radius.0",
    "size.control.md",
    "space.2",
    "space.3",
    "space.4",
    "space.5",
    "surface.card",
    "surface.raised",
    "surface.subtle",
    "text.muted",
    "text.primary",
    "type.size.lg",
    "type.size.xs",
    "type.weight.bold"
  ],
  "responsive": {
    "strategy": "Intrinsic layout with logical properties, responsive grids, and container queries where useful.",
    "breakpoints": [
      "sm",
      "md",
      "lg"
    ]
  },
  "slots": [
    "label",
    "content",
    "actions"
  ],
  "examples": [
    {
      "id": "date-picker-default",
      "name": "Standalone",
      "props": {
        "label": "Date Picker",
        "variant": "Example",
        "value": "Example",
        "min": "Example",
        "max": "Example"
      },
      "htmlFile": "example.html",
      "notes": "Consumer-authored reference, no automatic prop binding. Prefix IDs before repeating."
    }
  ],
  "allowedChildren": [
    "label",
    "content",
    "actions"
  ],
  "propertyContracts": {
    "label": {
      "type": "string",
      "required": false,
      "default": null,
      "allowedValues": null,
      "binding": {
        "selector": "label, [aria-label], summary, button, a, span",
        "html": "textContent or accessible name appropriate to the element"
      },
      "stateOwner": "consumer",
      "example": "Date Picker"
    },
    "variant": {
      "type": "string",
      "required": false,
      "default": null,
      "allowedValues": null,
      "binding": {
        "selector": ".sk-date-picker",
        "html": "see variantContracts; names do not imply data-variant support"
      },
      "stateOwner": "consumer",
      "example": "Example"
    },
    "value": {
      "type": "string",
      "required": false,
      "default": null,
      "allowedValues": null,
      "binding": {
        "selector": "input, [role=\"progressbar\"]",
        "html": "native value; progress aria-valuenow plus matching fill width"
      },
      "stateOwner": "consumer",
      "example": "Example"
    },
    "min": {
      "type": "string",
      "required": false,
      "default": null,
      "allowedValues": null,
      "binding": {
        "selector": "input",
        "html": "native min"
      },
      "stateOwner": "native",
      "example": "Example"
    },
    "max": {
      "type": "string",
      "required": false,
      "default": null,
      "allowedValues": null,
      "binding": {
        "selector": "input, [role=\"progressbar\"]",
        "html": "native max; progress aria-valuemax"
      },
      "stateOwner": "native",
      "example": "Example"
    }
  },
  "variantContracts": {
    "calendar": {
      "kind": "alias",
      "binding": null,
      "aliasOf": "base",
      "notes": "Compatible legacy name for the base appearance; no distinct attribute behavior."
    },
    "compact": {
      "kind": "alias",
      "binding": null,
      "aliasOf": "base",
      "notes": "Compatible legacy name for the base appearance; no distinct attribute behavior."
    }
  },
  "guidance": {
    "use": "Choose one date in a Sunday-first en-US calendar with a native input fallback.",
    "avoid": "Date ranges, bookings or a localization framework."
  },
  "slotContracts": {
    "label": {
      "owner": "consumer",
      "content": "Trusted authored semantic HTML; preserve child structure in example.html. Text data must be escaped.",
      "example": "example.html"
    },
    "content": {
      "owner": "consumer",
      "content": "Trusted authored semantic HTML; preserve child structure in example.html. Text data must be escaped.",
      "example": "example.html"
    },
    "actions": {
      "owner": "consumer",
      "content": "Trusted authored semantic HTML; preserve child structure in example.html. Text data must be escaped.",
      "example": "example.html"
    }
  },
  "historicalSources": [],
  "category": "Forms"
}
