{
  "$schema": "../../schemas/component.schema.json",
  "name": "Pricing Table",
  "description": "A commercial pricing comparison layout with plans and features.",
  "props": {
    "plans": "array",
    "currency": "string",
    "cadence": "string"
  },
  "variants": [
    "monthly",
    "annual",
    "comparison"
  ],
  "requiredFields": [
    "plans"
  ],
  "accessibility": {
    "semantics": "Use native semantic elements in Pricing Table templates before ARIA.",
    "keyboard": "Interactive descendants must be reachable by keyboard and expose visible focus.",
    "focus": "Uses :focus-visible styles in component CSS.",
    "notes": [
      "Color is never the only communication channel.",
      "Motion must respect prefers-reduced-motion where animation exists."
    ]
  },
  "tokenDependencies": [
    "surface.page",
    "surface.card",
    "text.primary",
    "text.secondary",
    "border.default",
    "focus.ring",
    "space.4",
    "radius.1"
  ],
  "responsive": {
    "strategy": "Intrinsic layout with logical properties, responsive grids, and container queries where useful.",
    "breakpoints": [
      "sm",
      "md",
      "lg"
    ]
  },
  "slots": [
    "plans",
    "feature-rows"
  ],
  "examples": [
    {
      "name": "Default",
      "props": {
        "label": "Pricing Table",
        "variant": "monthly"
      },
      "notes": "Use semantic landmarks and tokenized styling."
    }
  ],
  "allowedChildren": [
    "plans",
    "feature-rows"
  ]
}
