{
  "$schema": "../../schemas/component.schema.json",
  "name": "Footer",
  "description": "A semantic page footer for navigation, metadata, and legal links.",
  "props": {
    "brand": "string",
    "links": "array",
    "legal": "string"
  },
  "variants": [
    "simple",
    "columns",
    "product"
  ],
  "requiredFields": [
    "brand"
  ],
  "accessibility": {
    "semantics": "Use native semantic elements in Footer templates before ARIA.",
    "keyboard": "No keyboard behavior required unless interactive content is slotted.",
    "focus": "Inherits global focus styles for slotted links or controls.",
    "notes": [
      "Color is never the only communication channel.",
      "Motion must respect prefers-reduced-motion where animation exists."
    ]
  },
  "tokenDependencies": [
    "surface.page",
    "surface.card",
    "text.primary",
    "text.secondary",
    "border.default",
    "focus.ring",
    "space.4",
    "radius.1"
  ],
  "responsive": {
    "strategy": "Intrinsic layout with logical properties, responsive grids, and container queries where useful.",
    "breakpoints": [
      "sm",
      "md",
      "lg"
    ]
  },
  "slots": [
    "brand",
    "links",
    "legal"
  ],
  "examples": [
    {
      "name": "Default",
      "props": {
        "label": "Footer",
        "variant": "simple"
      },
      "notes": "Use semantic landmarks and tokenized styling."
    }
  ],
  "allowedChildren": [
    "brand",
    "links",
    "legal"
  ]
}
