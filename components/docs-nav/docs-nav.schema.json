{
  "$schema": "../../schemas/component.schema.json",
  "name": "Docs Nav",
  "description": "A documentation navigation landmark for section trees and active states.",
  "props": {
    "links": "array",
    "current": "string"
  },
  "variants": [
    "sidebar",
    "inline",
    "mobile"
  ],
  "requiredFields": [
    "links"
  ],
  "accessibility": {
    "semantics": "Use native semantic elements in Docs Nav templates before ARIA.",
    "keyboard": "Interactive descendants must be reachable by keyboard and expose visible focus.",
    "focus": "Uses :focus-visible styles in component CSS.",
    "notes": [
      "Color is never the only communication channel.",
      "Motion must respect prefers-reduced-motion where animation exists."
    ]
  },
  "tokenDependencies": [
    "surface.page",
    "surface.card",
    "text.primary",
    "text.secondary",
    "border.default",
    "focus.ring",
    "space.4",
    "radius.1"
  ],
  "responsive": {
    "strategy": "Intrinsic layout with logical properties, responsive grids, and container queries where useful.",
    "breakpoints": [
      "sm",
      "md",
      "lg"
    ]
  },
  "slots": [
    "links"
  ],
  "examples": [
    {
      "name": "Default",
      "props": {
        "label": "Docs Nav",
        "variant": "sidebar"
      },
      "notes": "Use semantic landmarks and tokenized styling."
    }
  ],
  "allowedChildren": [
    "links"
  ]
}
