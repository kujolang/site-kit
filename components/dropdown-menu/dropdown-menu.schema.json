{
  "$schema": "../../schemas/component.schema.json",
  "name": "Dropdown Menu",
  "description": "A menu button with grouped actions.",
  "props": {
    "label": "string optional",
    "variant": "default|compact"
  },
  "variants": [
    "default",
    "compact"
  ],
  "requiredFields": [],
  "accessibility": {
    "semantics": "Use native semantic elements before ARIA.",
    "keyboard": "Interactive descendants must be reachable by keyboard and expose visible focus.",
    "focus": "Uses :focus-visible styles in component CSS.",
    "notes": [
      "Color is never the only communication channel.",
      "Motion must respect prefers-reduced-motion where animation exists."
    ]
  },
  "tokenDependencies": [
    "action.primary",
    "action.primaryText",
    "border.1",
    "border.default",
    "focus.offset",
    "focus.ring",
    "focus.width",
    "space.2",
    "space.3",
    "surface.card",
    "text.primary",
    "z.dropdown"
  ],
  "responsive": {
    "strategy": "Intrinsic layout with logical properties, responsive grids, and container queries where useful.",
    "breakpoints": [
      "sm",
      "md",
      "lg"
    ]
  },
  "slots": [
    "label",
    "content",
    "actions"
  ],
  "examples": [
    {
      "id": "dropdown-menu-default",
      "name": "Standalone",
      "props": {
        "label": "Actions",
        "variant": "default"
      },
      "htmlFile": "example.html",
      "notes": "Consumer-authored reference, no automatic prop binding. Prefix IDs before repeating."
    }
  ],
  "allowedChildren": [
    "label",
    "content",
    "actions"
  ],
  "behavior": {
    "optionalScript": "dist/sitekit.js",
    "hooks": [
      "aria-haspopup=menu",
      "aria-expanded",
      "role=menu",
      "role=menuitem"
    ],
    "keyboard": "ArrowDown opens and focuses the first item; ArrowUp/ArrowDown, Home, End, Escape, and Tab are supported.",
    "dismissal": "Escape, Tab, and outside click close the menu and Escape restores focus to the trigger."
  },
  "propertyContracts": {
    "label": {
      "type": "string",
      "required": false,
      "default": null,
      "allowedValues": null,
      "binding": {
        "selector": "label, [aria-label], summary, button, a, span",
        "html": "textContent or accessible name appropriate to the element"
      },
      "stateOwner": "consumer",
      "example": "Dropdown Menu"
    },
    "variant": {
      "type": "string",
      "required": false,
      "default": null,
      "allowedValues": [
        "default",
        "compact"
      ],
      "binding": {
        "selector": ".sk-dropdown-menu",
        "html": "see variantContracts; names do not imply data-variant support"
      },
      "stateOwner": "consumer",
      "example": "default"
    }
  },
  "variantContracts": {
    "default": {
      "kind": "alias",
      "binding": null,
      "aliasOf": "base",
      "notes": "Compatible legacy name for the base appearance; no distinct attribute behavior."
    },
    "compact": {
      "kind": "alias",
      "binding": null,
      "aliasOf": "base",
      "notes": "Compatible legacy name for the base appearance; no distinct attribute behavior."
    }
  },
  "guidance": {
    "use": "Offer a button-triggered menu of commands with keyboard movement.",
    "avoid": "Navigation lists or a complete nested/typeahead menu system."
  },
  "slotContracts": {
    "label": {
      "owner": "consumer",
      "content": "Trusted authored semantic HTML; preserve child structure in example.html. Text data must be escaped.",
      "example": "example.html"
    },
    "content": {
      "owner": "consumer",
      "content": "Trusted authored semantic HTML; preserve child structure in example.html. Text data must be escaped.",
      "example": "example.html"
    },
    "actions": {
      "owner": "consumer",
      "content": "Trusted authored semantic HTML; preserve child structure in example.html. Text data must be escaped.",
      "example": "example.html"
    }
  },
  "historicalSources": [],
  "category": "Actions"
}
