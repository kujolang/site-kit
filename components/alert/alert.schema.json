{
  "$schema": "../../schemas/component.schema.json",
  "name": "Alert",
  "description": "A status message for success, warning, error, or neutral feedback.",
  "props": {
    "label": "string optional",
    "variant": "neutral|compact|danger|warning|success"
  },
  "variants": [
    "neutral",
    "compact",
    "danger",
    "warning",
    "success"
  ],
  "requiredFields": [],
  "accessibility": {
    "semantics": "Use native semantic elements before ARIA.",
    "keyboard": "Interactive descendants must be reachable by keyboard and expose visible focus.",
    "focus": "Uses :focus-visible styles in component CSS.",
    "notes": [
      "Color is never the only communication channel.",
      "Motion must respect prefers-reduced-motion where animation exists."
    ]
  },
  "tokenDependencies": [
    "border.1",
    "border.default",
    "border.heavy",
    "focus.offset",
    "focus.ring",
    "focus.width",
    "font.mono",
    "radius.0",
    "size.control.sm",
    "space.3",
    "space.4",
    "state.danger",
    "state.info",
    "state.success",
    "state.warning",
    "surface.card",
    "text.danger",
    "text.primary",
    "text.secondary",
    "text.success",
    "text.warning",
    "type.weight.bold"
  ],
  "responsive": {
    "strategy": "Intrinsic layout with logical properties, responsive grids, and container queries where useful.",
    "breakpoints": [
      "sm",
      "md",
      "lg"
    ]
  },
  "slots": [
    "label",
    "content",
    "actions"
  ],
  "examples": [
    {
      "id": "alert-default",
      "name": "Standalone",
      "props": {
        "variant": "warning"
      },
      "htmlFile": "example.html",
      "notes": "Consumer-authored reference, no automatic prop binding. Prefix IDs before repeating."
    },
    {
      "id": "alert-neutral",
      "name": "neutral",
      "props": {
        "variant": "neutral"
      },
      "htmlFile": "example-neutral.html",
      "notes": "Consumer-authored reference, no automatic prop binding. Prefix IDs before repeating."
    },
    {
      "id": "alert-danger",
      "name": "danger",
      "props": {
        "variant": "danger"
      },
      "htmlFile": "example-danger.html",
      "notes": "Consumer-authored reference, no automatic prop binding. Prefix IDs before repeating."
    },
    {
      "id": "alert-warning",
      "name": "warning",
      "props": {
        "variant": "warning"
      },
      "htmlFile": "example-warning.html",
      "notes": "Consumer-authored reference, no automatic prop binding. Prefix IDs before repeating."
    },
    {
      "id": "alert-success",
      "name": "success",
      "props": {
        "variant": "success"
      },
      "htmlFile": "example-success.html",
      "notes": "Consumer-authored reference, no automatic prop binding. Prefix IDs before repeating."
    }
  ],
  "allowedChildren": [
    "label",
    "content",
    "actions"
  ],
  "propertyContracts": {
    "label": {
      "type": "string",
      "required": false,
      "default": null,
      "allowedValues": null,
      "binding": {
        "selector": "label, [aria-label], summary, button, a, span",
        "html": "textContent or accessible name appropriate to the element"
      },
      "stateOwner": "consumer",
      "example": "Alert"
    },
    "variant": {
      "type": "string",
      "required": false,
      "default": null,
      "allowedValues": [
        "neutral",
        "compact",
        "danger",
        "warning",
        "success"
      ],
      "binding": {
        "selector": ".sk-alert",
        "html": "see variantContracts; names do not imply data-variant support"
      },
      "stateOwner": "consumer",
      "example": "warning"
    }
  },
  "variantContracts": {
    "neutral": {
      "kind": "implemented",
      "binding": "[data-variant=\"neutral\"]",
      "aliasOf": null,
      "notes": "Apply the attribute on the source selector.",
      "example": "alert-neutral"
    },
    "compact": {
      "kind": "alias",
      "binding": null,
      "aliasOf": "base",
      "notes": "Compatible legacy name for the base appearance; no distinct attribute behavior."
    },
    "danger": {
      "kind": "implemented",
      "binding": "[data-variant=\"danger\"]",
      "aliasOf": null,
      "notes": "Apply the attribute on the source selector.",
      "example": "alert-danger"
    },
    "warning": {
      "kind": "implemented",
      "binding": "[data-variant=\"warning\"]",
      "aliasOf": null,
      "notes": "Apply the attribute on the source selector.",
      "example": "alert-warning"
    },
    "success": {
      "kind": "implemented",
      "binding": "[data-variant=\"success\"]",
      "aliasOf": null,
      "notes": "Apply the attribute on the source selector.",
      "example": "alert-success"
    }
  },
  "guidance": {
    "use": "Show a status notice with an exclamation marker and optional severity colors.",
    "avoid": "A blocking dialog or automatically dismissing notification."
  },
  "slotContracts": {
    "label": {
      "owner": "consumer",
      "content": "Trusted authored semantic HTML; preserve child structure in example.html. Text data must be escaped.",
      "example": "example.html"
    },
    "content": {
      "owner": "consumer",
      "content": "Trusted authored semantic HTML; preserve child structure in example.html. Text data must be escaped.",
      "example": "example.html"
    },
    "actions": {
      "owner": "consumer",
      "content": "Trusted authored semantic HTML; preserve child structure in example.html. Text data must be escaped.",
      "example": "example.html"
    }
  },
  "historicalSources": [],
  "category": "Feedback"
}
