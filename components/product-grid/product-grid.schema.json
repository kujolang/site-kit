{
  "$schema": "../../schemas/component.schema.json",
  "name": "Product Grid",
  "description": "A responsive product listing surface.",
  "props": {
    "products": "array",
    "filters": "array optional"
  },
  "variants": [
    "catalog",
    "compact",
    "comparison"
  ],
  "requiredFields": [
    "products"
  ],
  "accessibility": {
    "semantics": "Use native semantic elements in Product Grid templates before ARIA.",
    "keyboard": "No keyboard behavior required unless interactive content is slotted.",
    "focus": "Inherits global focus styles for slotted links or controls.",
    "notes": [
      "Color is never the only communication channel.",
      "Motion must respect prefers-reduced-motion where animation exists."
    ]
  },
  "tokenDependencies": [
    "surface.page",
    "surface.card",
    "text.primary",
    "text.secondary",
    "border.default",
    "focus.ring",
    "space.4",
    "radius.1"
  ],
  "responsive": {
    "strategy": "Intrinsic layout with logical properties, responsive grids, and container queries where useful.",
    "breakpoints": [
      "sm",
      "md",
      "lg"
    ]
  },
  "slots": [
    "products",
    "filters"
  ],
  "examples": [
    {
      "name": "Default",
      "props": {
        "label": "Product Grid",
        "variant": "catalog"
      },
      "notes": "Use semantic landmarks and tokenized styling."
    }
  ],
  "allowedChildren": [
    "products",
    "filters"
  ]
}
