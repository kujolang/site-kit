{
  "$schema": "../../schemas/component.schema.json",
  "name": "Comparison Table",
  "description": "A structured feature comparison table for commercial or docs pages.",
  "props": {
    "columns": "array",
    "rows": "array"
  },
  "variants": [
    "default",
    "compact",
    "sticky"
  ],
  "requiredFields": [
    "columns",
    "rows"
  ],
  "accessibility": {
    "semantics": "Use native semantic elements in Comparison Table templates before ARIA.",
    "keyboard": "No keyboard behavior required unless interactive content is slotted.",
    "focus": "Inherits global focus styles for slotted links or controls.",
    "notes": [
      "Color is never the only communication channel.",
      "Motion must respect prefers-reduced-motion where animation exists."
    ]
  },
  "tokenDependencies": [
    "surface.page",
    "surface.card",
    "text.primary",
    "text.secondary",
    "border.default",
    "focus.ring",
    "space.4",
    "radius.1"
  ],
  "responsive": {
    "strategy": "Intrinsic layout with logical properties, responsive grids, and container queries where useful.",
    "breakpoints": [
      "sm",
      "md",
      "lg"
    ]
  },
  "slots": [
    "columns",
    "rows"
  ],
  "examples": [
    {
      "name": "Default",
      "props": {
        "label": "Comparison Table",
        "variant": "default"
      },
      "notes": "Use semantic landmarks and tokenized styling."
    }
  ],
  "allowedChildren": [
    "columns",
    "rows"
  ]
}
