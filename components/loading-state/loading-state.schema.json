{
  "$schema": "../../schemas/component.schema.json",
  "name": "Loading State",
  "description": "A non-blocking loading placeholder that respects reduced motion.",
  "props": {
    "label": "string",
    "size": "sm|md|lg"
  },
  "variants": [
    "skeleton",
    "inline",
    "panel"
  ],
  "requiredFields": [
    "label"
  ],
  "accessibility": {
    "semantics": "Use native semantic elements in Loading State templates before ARIA.",
    "keyboard": "No keyboard behavior required unless interactive content is slotted.",
    "focus": "Inherits global focus styles for slotted links or controls.",
    "notes": [
      "Color is never the only communication channel.",
      "Motion must respect prefers-reduced-motion where animation exists."
    ]
  },
  "tokenDependencies": [
    "surface.page",
    "surface.card",
    "text.primary",
    "text.secondary",
    "border.default",
    "focus.ring",
    "space.4",
    "radius.1"
  ],
  "responsive": {
    "strategy": "Intrinsic layout with logical properties, responsive grids, and container queries where useful.",
    "breakpoints": [
      "sm",
      "md",
      "lg"
    ]
  },
  "slots": [
    "label",
    "skeleton"
  ],
  "examples": [
    {
      "name": "Default",
      "props": {
        "label": "Loading State",
        "variant": "skeleton"
      },
      "notes": "Use semantic landmarks and tokenized styling."
    }
  ],
  "allowedChildren": [
    "label",
    "skeleton"
  ]
}
