{
  "$schema": "../../schemas/component.schema.json",
  "name": "Header",
  "description": "A top-level site header with navigation and primary action.",
  "props": {
    "brand": "string",
    "nav": "array",
    "actions": "array optional"
  },
  "variants": [
    "default",
    "docs",
    "commerce"
  ],
  "requiredFields": [
    "brand"
  ],
  "accessibility": {
    "semantics": "Use native semantic elements in Header templates before ARIA.",
    "keyboard": "Interactive descendants must be reachable by keyboard and expose visible focus.",
    "focus": "Uses :focus-visible styles in component CSS.",
    "notes": [
      "Color is never the only communication channel.",
      "Motion must respect prefers-reduced-motion where animation exists."
    ]
  },
  "tokenDependencies": [
    "surface.page",
    "surface.card",
    "text.primary",
    "text.secondary",
    "border.default",
    "focus.ring",
    "space.4",
    "radius.1"
  ],
  "responsive": {
    "strategy": "Intrinsic layout with logical properties, responsive grids, and container queries where useful.",
    "breakpoints": [
      "sm",
      "md",
      "lg"
    ]
  },
  "slots": [
    "brand",
    "nav",
    "actions"
  ],
  "examples": [
    {
      "name": "Default",
      "props": {
        "label": "Header",
        "variant": "default"
      },
      "notes": "Use semantic landmarks and tokenized styling."
    }
  ],
  "allowedChildren": [
    "brand",
    "nav",
    "actions"
  ]
}
