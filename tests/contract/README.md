# Contract tests

`npm run validate` verifies all component schemas, semantic templates, version consistency, package exports, CSS layer order, focus and reduced-motion rules, font paths, generated manifest hashes, required licenses, representative page semantics, the complete generated component index, and every local Markdown link.
