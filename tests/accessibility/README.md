# Accessibility tests

`npm run validate` checks source semantics and required accessibility contracts. `npm run browser:test` uses axe-core plus keyboard interaction checks against representative pages in Chromium, Firefox, and WebKit. Lens provides an independent rendered-page accessibility report. These gates cover SiteKit reference compositions, not arbitrary consumer accessibility certification.
