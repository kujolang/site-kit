# accessibility Standard

SiteKit targets WCAG 2.2 AA as the baseline. Prefer native HTML before ARIA. Interactive components require keyboard operation, visible focus, hover, active, disabled, and loading states where applicable. Motion must respect prefers-reduced-motion. Color cannot be the only status indicator. Dialogs require focus management and a semantic title. Forms require visible labels, help text, errors, and useful autocomplete values.
