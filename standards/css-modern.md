# Modern CSS standard

Use CSS custom properties, the reset/tokens/base/components/utilities layer order, logical properties, intrinsic layout, container queries where useful, `:focus-visible`, and reduced-motion fallbacks. Component CSS must use tokens for visual values. Preserve relative `./fonts/` URLs in the generated distribution and documented overflow behavior for tables and carousels.
