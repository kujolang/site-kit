# css-modern Standard

Use CSS custom properties, cascade layers, logical properties, intrinsic layout, container queries where useful, focus-visible, and reduced-motion fallbacks. Component CSS must use tokens for visual values.
