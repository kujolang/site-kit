# Performance standard

Prefer static HTML and CSS, local assets, minimal optional JavaScript, stable layout dimensions, and responsive images. Consumers should copy the complete `dist/` directory, serve compressed assets where appropriate, and avoid adding render-blocking third-party dependencies.
