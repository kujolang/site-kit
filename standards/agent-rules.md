# Agent rules standard

Agents must read [DESIGN.md](../DESIGN.md), use component schemas, preserve semantic HTML, use tokens, run formatting, generation, validation, browser, and release-content checks, and repair source components when reusable patterns fail. Generated output must come from its source or generator. Keep the source-vendored `dist/` contract and never enable npm publication incidentally.
